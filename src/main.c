/*
 * mini-httpd —— 骨架（T0.2 的起点）
 * 真正的实现按 TASKS.md 的 M0-T0.3 往下做。
 */
#include <stdio.h>

int main(int argc, char **argv) {
    (void)argc;
    (void)argv;
    fprintf(stderr, "mini-httpd: 骨架就绪。下一步见 TASKS.md 的 M0-T0.3（最小 TCP echo）\n");
    return 0;
}
